/**
 * The brand's colours where CSS variables cannot reach: the app icon, the web
 * app manifest and the browser's theme colour (ADR-0016). Restated from
 * `packages/ui/src/theme.css`; change them there first.
 */

/** `--color-accent`, oklch(50% 0.09 192). */
export const BRAND_TEAL = "#007370";
/** `--color-accent-ink`, oklch(99% 0.004 190). */
export const BRAND_ON_TEAL = "#F9FDFC";
/** `--color-surface`, oklch(98.3% 0.006 190): the page behind the app while it opens. */
export const BRAND_CANVAS = "#F5FBFA";
